@model IEnumerable<AgriEnergyConnect.Models.Product>

@{
    ViewData["Title"] = "View Products";
}

<h2>Products</h2>

<table class="table">
    <thead>
        <tr>
            <th>Product Name</th>
            <th>Description</th>
            <th>Category</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.PoductName</td>
                <td>@product.ProductDes</td>
                <td>@product.Catergry</td>
            </tr>
        }
    </tbody>
</table>
